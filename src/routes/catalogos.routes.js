import { Router } from 'express'

// Router de /api/catalogos.
// Este modulo agrupa endpoints de catalogos (categorias, etiquetas, etc.).
const router = Router()

// TODO: agregar endpoints reales de catalogos.

export default router
