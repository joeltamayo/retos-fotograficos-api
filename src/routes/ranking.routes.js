import { Router } from 'express'

// Router de /api/ranking.
// Aqui se publican endpoints para tablas de ranking.
const router = Router()

// TODO: agregar endpoints reales de ranking.

export default router
