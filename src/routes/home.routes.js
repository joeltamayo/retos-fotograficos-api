import { Router } from 'express'

// Router de /api/home.
// Mantener las rutas de Home aisladas facilita su mantenimiento.
const router = Router()

// TODO: agregar endpoints reales de Home.

export default router
