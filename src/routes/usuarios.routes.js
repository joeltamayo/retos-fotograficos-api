import { Router } from 'express'

// Router de /api/usuarios.
// Centraliza endpoints de perfil, participaciones y gestion de usuario.
const router = Router()

// TODO: agregar endpoints reales de usuarios.

export default router
