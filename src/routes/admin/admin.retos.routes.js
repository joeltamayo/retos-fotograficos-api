import { Router } from 'express'

// Router de /api/admin/retos.
// Reservado para operaciones administrativas de retos.
const router = Router()

// TODO: agregar endpoints reales de administracion de retos.

export default router
