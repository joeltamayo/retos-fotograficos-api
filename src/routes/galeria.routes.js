import { Router } from 'express'

// Router de /api/galeria.
// Expone endpoints de consulta de galeria publica.
const router = Router()

// TODO: agregar endpoints reales de galeria.

export default router
