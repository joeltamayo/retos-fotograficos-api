import { Router } from 'express'

// Router de /api/retos.
// Aqui se concentraran endpoints publicos de retos.
const router = Router()

// TODO: agregar endpoints reales de retos.

export default router
